# Copy this file to .env, or just run ./setup.sh, which does this for
# you automatically.

# Where the backend API is running. If you're running school-backend
# locally via its own setup.sh, this is correct as-is.
VITE_NODE_HOST_APP=http://localhost:8000
